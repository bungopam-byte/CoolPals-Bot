# CoolPals Bot Website

Static website for CoolPals Bot.

Deploy the contents of this top-level `website/` folder to Cloudflare Pages.

No build command is required.

Cloudflare Pages settings:

- Framework preset: None
- Build command: leave blank
- Output directory: `/`

Important: deploy this folder, not the nested `website/website/` copy if it exists locally.
